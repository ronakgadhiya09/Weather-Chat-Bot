# Core web framework
fastapi==0.103.1
uvicorn==0.23.2
starlette==0.27.0

# Core dependencies for the weather bot
agno>=2.0.0

# HTTP client - let pip resolve the version
httpx>=0.25.0
requests>=2.32.0

# Pydantic with safe versions that have pre-compiled wheels
pydantic>=2.3.0,<3.0.0
pydantic-settings>=2.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# AI/LLM libraries
openai>=1.0.0
groq>=0.8.0

# Essential utilities
python-dotenv>=1.0.0
click>=8.0.0
rich>=13.0.0

# Core Python utilities
certifi>=2023.0.0
charset-normalizer>=3.0.0
idna>=3.0.0
urllib3>=2.0.0
python-dateutil>=2.8.0

# JSON and data formats
Jinja2>=3.1.0
MarkupSafe>=3.0.0
PyYAML>=6.0.0

# Async support
anyio>=3.7.0

# Database
SQLAlchemy>=2.0.0

# Image processing
pillow>=10.0.0

# Web utilities
python-multipart>=0.0.20

# Replace problematic packages with safer alternatives or remove if not needed
# Use only packages with known good wheel support
